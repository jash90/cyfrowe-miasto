import { Image } from 'expo-image';
import React from 'react';

const City = () => {
    return <Image source={require('@/assets/images/city.png')} style={{ width: 360, height: 94, marginTop: 16, marginBottom: 10 }} />
}

export default City
